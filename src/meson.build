subdir('compat')
subdir('core')
subdir('platform')
subdir('protocol')
subdir('supplemental')
subdir('transport')

nng_src = \
files('nng.c') + \
compat_src + \
core_src + \
platform_src + \
protocol_src + \
supplemental_src + \
transport_src
